import turtle as t
import random as r
def draw_square():
    t.up()
    t.goto(x,y)
    t.down()
    t.fillcolor(c)
    t.begin_fill()
    for i in range(4):
        t.forward(100)
        t.left(90)
    t.end_fill()

colorL=[]
for i in range(20):
    colorL.append((r.random(),r.random(),r.random()))
for c in colorL:
    x=r.randint(-300,300)
    y=r.randint(-300,300)
    draw_square()
